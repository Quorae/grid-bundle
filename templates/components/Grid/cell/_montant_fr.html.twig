{#
 # Grid cell · Formatter::MontantFr — montant français.
 #
 # Délégation au filtre `montant_fr` (Quorae\GridBundle\Twig\GridFormattingExtension) :
 # `1 234,56` avec espace insécable comme séparateur de milliers et
 # tiret cadratin pour les valeurs nulles/zéro.
 #
 # Le CSS parent (.grid-table td.num) applique déjà tabular-nums + mono.
 #
 # Params : value (float|int|string numeric|null), row, column
 #}
{{- value|montant_fr -}}
